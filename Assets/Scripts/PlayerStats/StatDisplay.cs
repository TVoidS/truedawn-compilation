using UnityEngine;

public class StatDisplay : MonoBehaviour
{
    public StatEnums DisplayedStat;

    // Start is called before the first frame update
    void Start()
    {
        // TODO:
        // Connect to the object's display values
        // Send a Listenter to SkillController
    }

    // TODO:
    // Add a method that updates the display on event.
}
